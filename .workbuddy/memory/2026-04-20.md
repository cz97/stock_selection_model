# 2026-04-20

## 笨总体系材料对齐

用户把另一会话的讨论迁移过来，并补充三份材料：
- `超景气价值投机体系总结.md`：笨总十期视频的二次提炼，质量高
- `dynamics.md`：微博动态原始语料（31万字）
- `purified_videos.md`：视频字幕原始语料（175万字）

两份原始语料过大不可通读，后续按需 grep。

## 关键认知修正（相对上次讨论）

1. **入口方向反了**：之前假设"用户输入主题 → 五层拆解"，但笨总的真实路径是"事件扫描 → 超景气板块 → 纯度筛选 → 打分"。事件扫描才是入口。
2. **打分模型不硬复刻**：笨总自己说"比值是主观经验、平时不用"。保留七维结构，但权重在本项目迭代中校准。
3. **止盈体系可复用**：第八期的止盈信号（换手率/涨幅/股东人数/减持）数据全部可得，应做进同一 Expert，作为 `/exit-monitor` 入口。

## Expert 五入口骨架（v2 替代旧 v1）

- `/analyze-theme <主题>` - MVP
- `/score <股票>` - MVP
- `/check-double <股票>` - v2（笨九双击检查）
- `/scan-events` - v2
- `/exit-monitor <持仓>` - v2

共享基础层：事件过滤器、纯度筛选器、估值位置计算器、风险公告扫描、流动性系数。

## 今日产出

- 写了项目 README.md，钉住项目初心、五入口规划、数据源选型、七维打分自动化立场、免责声明。
- 用户确认"先做 MVP + 人喂事件模式"的方向（待最终拍板）。

## 下一步

等用户拍板 MVP 范围后，开始写 SKILL.md（`analyze-theme` 和 `score` 两个入口），以及基础层的具体工作流。
